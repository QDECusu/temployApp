import React, { Component } from "react";
import { AvoidKeyboard } from "./utils";
import Login from "./Login";

export default class Root extends Component {
  render() {
    return <Login />;
  }
}
